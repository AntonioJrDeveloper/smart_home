import 'package:flutter/material.dart';

class SmartHomeRadius {
  SmartHomeRadius._();
  static const Radius xs = Radius.circular(12);
  static const Radius card = Radius.circular(18);
  static const Radius small = Radius.circular(50);
  static const Radius smallX = Radius.circular(100);
  static const Radius smallXX = Radius.circular(150);
}
